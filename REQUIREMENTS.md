# 需求变更记录

本文档用于记录本项目每次需求、约定和实现变更的背景，方便后续回看当时的业务逻辑和技术决策。

## 记录规则

- 只要用户提出新需求、调整需求、技术选型变化或影响代码结构的改动，都要同步更新本文档。
- 记录内容使用中文，包含日期、需求来源、变更摘要、影响范围和后续待办。
- 不记录数据库密码、令牌、生产地址等敏感信息；如需说明配置，仅描述配置项名称和用途。

## 2026-06-26 初始化项目规范

### 需求来源

用户要求生成仓库贡献指南，并在当前项目中引入 Java 开发规范约束。

### 变更摘要

- 创建项目级 `AGENTS.md`，记录项目结构、开发命令、编码风格、测试规范和提交规范。
- 当前项目 Java 开发参考 `D:\download\阿里巴巴Java开发手册（公开版）.pdf`。
- 约定 Markdown 文档默认使用中文。

### 影响范围

- 后续 Java、MySQL、ORM、异常日志、安全和代码评审都需要参考项目级 `AGENTS.md`。

## 2026-06-26 初始化全栈项目骨架

### 需求来源

用户指定技术栈：JDK 21 LTS、Spring Boot 3.5.16、Vue 3.x、Node.js 22 LTS、MySQL 8.4 LTS、Docker Compose。

### 变更摘要

- 创建 `backend/` Spring Boot 后端骨架。
- 创建 `frontend/` Vue 3 + Vite + TypeScript 前端骨架。
- 创建 `docker-compose.yml`、`.env.example`、`.gitignore`、`.editorconfig` 和 `README.md`。
- 后端提供 `/api/health` 健康检查接口，前端首页调用该接口展示状态。

### 影响范围

- 项目采用前后端分离结构。
- 本地真实配置写入 `.env`，不提交到 Git。

## 2026-06-29 数据库脚本目录约定

### 需求来源

用户要求在后端和前端同级创建专门存放建表语句和初始化数据脚本的数据库文件夹。

### 变更摘要

- 创建 `database/README.md`。
- 在项目级 `AGENTS.md` 中约定后续建表语句、结构变更、索引调整和初始化数据脚本默认放入 `database/`。
- 建议按 `database/schema/`、`database/seed/`、`database/migration/` 拆分脚本。

### 影响范围

- 后续数据库相关 SQL 文件都应进入 `database/`，避免散落在后端、文档或临时目录。

## 2026-06-29 配置测试数据库并推送 GitHub

### 需求来源

用户提供测试数据库连接信息，并创建 GitHub 仓库 `wangwanpeng1103/dev_pro.git`。

### 变更摘要

- 新增 `backend/src/main/resources/application-local.yml`，通过环境变量读取本地数据库连接。
- 创建本地 `.env` 保存真实测试数据库配置，`.env` 被 `.gitignore` 排除。
- 将项目初始代码提交并推送到 GitHub `main` 分支。

### 影响范围

- 后端本地运行使用 `SPRING_PROFILES_ACTIVE=local`。
- 真实数据库密码只保存在本地 `.env`，不进入 Git 仓库。

## 2026-06-29 建立需求记录机制

### 需求来源

用户要求创建一个全局 Markdown 文件，用于保存每次需求汇总记录，并在项目规则中约定后续需求和改动都要同步记录。

### 变更摘要

- 创建 `REQUIREMENTS.md` 作为项目需求变更记录。
- 在项目级 `AGENTS.md` 中新增需求记录规则。

### 影响范围

- 后续每次用户提出需求、需求调整或代码改动，都要同步更新 `REQUIREMENTS.md`。

## 2026-06-29 绿云运维控制台需求与设计草案

### 需求来源

用户提出建设“绿云运维控制台”，要求先不要改代码，先整理需求和设计文档，用于确认是否存在遗漏。

### 变更摘要

- 明确平台需要用户体系、权限体系、项目模块管理和项目内功能树配置能力。
- 初始项目模块包含“用户管理”“mihotel”“ihotel”。
- `admin` 拥有全部权限，并可创建永久用户、临时用户、分配权限、新增项目模块、配置功能树。
- 临时用户默认只允许登录一天，并需要与永久用户区分展示。
- UI 风格以后统一参考第一张图和第三张图：深色品牌区或侧栏、浅色工作区、克制卡片和表单。
- 新增 `docs/ops-console-requirement-design.md`，记录需求拆解、页面规划、权限规则、数据模型草案和待确认问题。

### 影响范围

- 后续实现登录页、项目列表页、用户管理页、项目配置页、功能树配置页和项目运维页时，应优先参考该设计文档。
- 第二张图只作为项目列表信息结构参考，不作为最终 UI 风格基准。

## 2026-06-29 绿云运维控制台第一期骨架确认

### 需求来源

用户确认第一期权限边界，并要求先把骨架搭出来，`mihotel` 和 `ihotel` 的具体功能清单后续再补。

### 变更摘要

- 权限模型采用用户直接授权项目，一个用户可以拥有多个项目权限。
- 第一期不引入角色体系。
- 临时用户有效期通过参数配置，可以自定义。
- 项目内功能树暂时不做权限控制，只控制外层项目模块访问权限。
- 暂不实现登录失败锁定、验证码和操作审计日志。
- 功能树节点需要支持外部链接，并预留后续单点登录能力。
- 新增数据库建表脚本和初始化脚本，放在 `database/schema/` 与 `database/seed/`。
- 后端新增用户、项目模块、功能树配置接口骨架，当前以内存数据承载。
- 前端新增登录、项目列表、admin 管理区和项目运维页骨架，并统一为第一张和第三张图接近的 UI 风格。

### 影响范围

- 后续接入 MySQL 时，需要把当前内存服务替换为数据库持久化实现。
- `mihotel`、`ihotel` 的具体功能节点清单待用户下次提供后再补齐。

## 2026-06-29 运维控制台接入测试 MySQL

### 需求来源

用户要求将数据库脚本执行到测试环境，并把后端代码从内存骨架调整为真实查询 MySQL 数据。

### 变更摘要

- 将 `database/schema/20260629_001_create_ops_console_tables.sql` 和 `database/seed/20260629_001_init_ops_console_seed.sql` 执行到测试库。
- SQL 脚本调整为可重复执行，避免重复初始化时报错。
- 后端新增 Spring JDBC 数据访问层，用户、项目模块、用户项目授权和功能树改为查询 MySQL。
- 登录开始校验数据库中的用户状态、临时用户过期时间和简单密码哈希。
- 新增 `scripts/run-backend-local.ps1`，用于本地读取 `.env` 并启动后端。

### 影响范围

- 当前后端不再依赖内存 Map 作为主数据来源。
- 第一阶段仍使用简单 `{noop}` 密码格式，后续需要接入更安全的密码哈希策略。

## 2026-06-29 配置本地 IDEA 启动环境变量

### 需求来源

用户使用 IDEA 直接启动后端时报 DataSource 配置缺失，要求把环境变量配置进去。

### 变更摘要

- 本地 `.env` 已包含 `SPRING_PROFILES_ACTIVE=local`、数据库 URL、用户名和密码。
- 新增本机 IDEA Run Configuration：`DevProApplication-local`，用于启动后端时自动启用 `local` profile 并注入数据库环境变量。
- 该配置位于 `backend/.idea/runConfigurations/`，属于本地忽略目录，不提交到 Git。

### 影响范围

- 后续在 IDEA 中选择 `DevProApplication-local` 启动即可加载测试库配置。
- 真实数据库密码仍不进入 Git 仓库。

## 2026-06-29 后端默认读取本地 `.env`

### 需求来源

用户再次使用 IDEA 原启动配置启动后端，日志仍显示 `No active profile set`，导致 DataSource 没有 URL。

### 变更摘要

- 调整 `backend/src/main/resources/application.yml`，默认启用 `local` profile。
- 配置 Spring Boot 自动尝试导入 `.env` 和 `../.env`，兼容从项目根目录或 `backend/` 目录启动。
- 保持真实数据库密码只存在本地 `.env`，不提交到 Git。

### 影响范围

- 后续直接运行 `DevProApplication` 也会默认加载 `application-local.yml`。
- Docker 或其他环境仍可通过 `SPRING_PROFILES_ACTIVE` 覆盖默认 profile。

## 2026-06-29 增加后端启动成功日志

### 需求来源

用户反馈后端启动后不容易判断是否成功，要求在日志中增加明确的项目启动成功提示。

### 变更摘要

- 在 `DevProApplication` 中监听 `ApplicationReadyEvent`。
- 应用完全启动后输出“绿云运维控制台后端启动成功”。
- 日志同时打印本地访问地址、健康检查地址和当前 profile。

### 影响范围

- 后续本地启动时，可以通过醒目的启动成功日志确认服务已经可用。

## 2026-06-29 调整项目入口页导航结构

### 需求来源

用户反馈登录进入项目模块页面时，不需要立即展示左侧树形结构；只有点击进入具体项目后，才应该出现该项目内的功能树。

### 变更摘要

- 登录后的项目模块页改为顶部栏加项目卡片布局，不再显示全局左侧侧边栏。
- 用户管理、mihotel、ihotel 都作为项目入口卡片展示。
- 点击进入任一项目后，再展示左侧功能树和右侧工作区。
- 用户管理模块进入后仍保留用户、项目模块、功能树三个管理页签。

### 影响范围

- 后续新增项目模块时，默认先展示在项目入口卡片区。
- 后续新增项目内功能节点时，只在进入对应项目后的左侧功能树中展示。

## 2026-06-29 升级本机 Node.js 到 22.12+

### 需求来源

前端构建时 Vite 提示当前 Node.js 版本低于推荐要求，用户要求升级到 `22.12+`。

### 变更摘要

- 从 Node.js 官方发布目录选择 `v22.23.1` Windows x64 版本。
- 下载发布包并校验官方 SHA256 后，替换本机 `D:\dev\nodejs`。
- 原 `D:\dev\nodejs` 已备份为 `D:\dev\nodejs-backup-20260629131906`。

### 影响范围

- 当前命令行 `node -v` 返回 `v22.23.1`。
- 当前命令行使用 `npm.cmd -v` 返回 `10.9.8`。
- Windows PowerShell 直接执行 `npm` 仍可能受脚本执行策略影响，项目命令建议继续使用 `npm.cmd`。

## 2026-06-29 调整项目模块卡片交互

### 需求来源

用户反馈项目模块页需要更简洁：去掉说明文案和卡片内“进入”按钮，并将“可运维项目”标题移动到左上方。

### 变更摘要

- 项目入口页去除标题下方说明文案。
- 项目卡片去除描述文案，只保留图标和项目名称。
- 项目卡片去除单独“进入”按钮，改为点击整张卡片进入项目。
- 项目入口内容区调整为左上对齐展示。

### 影响范围

- 后续项目模块入口默认采用整卡点击交互。
- 后续新增项目描述仍可保留在数据中，但项目入口卡片暂不展示描述。

## 2026-06-29 去除登录页品牌介绍文案

### 需求来源

用户反馈登录页左侧标题下方的品牌介绍文案不需要展示，并要求保持原有布局不变。

### 变更摘要

- 删除登录页左侧标题下方“集中管理项目入口、用户授权和可扩展运维功能树。”文案。
- 移除对应 `.brand-copy` 样式。
- 其他登录页布局、卡片、功能说明列表保持不变。

### 影响范围

- 登录页左侧品牌区更简洁，后续如需补充说明，应优先放在下方说明卡片或功能列表中。

## 2026-06-29 登录页品牌介绍区域保留空白占位

### 需求来源

用户进一步说明，登录页左侧标题下方的文案需要去掉，但原位置应保持空白占位，避免下方内容上移导致底部过空。

### 变更摘要

- 在标题下方新增不可见占位区域。
- 占位区域保留原介绍文案的大致高度和间距。
- 下方说明卡片、功能列表和登录表单布局保持不变。

### 影响范围

- 后续调整登录页时，不应随意删除该空白占位，除非重新设计左侧整体垂直排布。

## 2026-06-29 调整项目入口页内容带

### 需求来源

用户反馈项目入口页“可运维项目”标题需要更换样式，并要求项目模块卡片只展示在页面中间工作区域内。

### 变更摘要

- 将“可运维项目”标题调整为白底、细边框、左侧蓝色强调线的标题块。
- 项目模块卡片区从页面左侧贴边改为居中内容带展示。
- 三个项目模块卡片在内容带内按三列自适应铺开。

### 影响范围

- 后续新增项目模块时，入口卡片仍应限制在该居中内容带内展示。

## 2026-06-29 项目入口页支持四列项目卡片

### 需求来源

用户反馈“可运维项目”标题块需要保持在左上方，同时中间项目模块区域一行需要能展示四个项目。

### 变更摘要

- 项目入口容器保持左上方标题块位置。
- 项目模块网格调整为四列布局。
- 项目卡片最小宽度下调，保证中间区域可一次容纳四个项目。

### 影响范围

- 后续新增第四个项目模块时，将默认与前三个项目在同一行展示。

## 2026-06-29 左移项目入口标题块

### 需求来源

用户反馈“可运维项目”标题块仍需再向左移动到指定位置，项目卡片区域保持不变。

### 变更摘要

- 项目入口页标题块单独向左偏移。
- 项目模块四列网格位置和宽度保持不变。

### 影响范围

- 后续调整项目入口页时，需要区分标题块位置和项目卡片网格位置。

## 2026-06-29 调整登录卡片标题

### 需求来源

用户反馈登录卡片标题中的“进入”两个字需要去掉，并要求“绿云运维控制台”在卡片中居中显示。

### 变更摘要

- 登录卡片标题从“进入绿云运维控制台”调整为“绿云运维控制台”。
- 新增登录标题专用样式，使标题居中显示。

### 影响范围

- 仅影响登录卡片标题展示，不影响左侧品牌区和其它页面标题。

## 2026-06-29 前端展示接口错误信息

### 需求来源

用户反馈前端调用接口报错时不应只提示 HTTP 状态码，例如登录失败只显示 400，无法判断具体原因。

### 变更摘要

- 前端请求封装在 HTTP 非 2xx 时，也会优先读取接口返回 JSON 中的 `message` 并展示。
- 当前接口返回格式异常时，前端提示“接口返回格式错误”。
- 登录接口继续返回后端具体业务错误信息，例如“用户不存在、已禁用或已过期”“用户名或密码错误”。

### 影响范围

- 后续所有前端 API 调用失败时，默认展示后端业务错误信息。
- 后端接口应继续使用统一 `ApiResponse` 结构返回清晰的中文错误信息。

## 2026-06-29 登录错误改为弹框提示

### 需求来源

用户反馈登录错误不应显示在表单底部红字中，需要弹出一个小型提示框，样式与当前登录页风格相近，并提供“确定”按钮关闭。

### 变更摘要

- 登录失败时显示居中的小型错误弹框。
- 弹框使用浅色遮罩、白底卡片、深色圆形提示标识和蓝色确认按钮。
- 点击“确定”后清空错误信息并关闭弹框。
- 移除登录表单底部红字错误展示。

### 影响范围

- 登录页错误提示统一改为弹框交互，后续其它关键错误提示可参考该样式。

## 2026-06-29 确认 REQUIREMENTS 为唯一核心产品文档

### 需求来源

用户要求检查项目级 `AGENTS.md`，确认后续是否存在“所有工作先参考主 Markdown 文档，如有差异再向用户确认”的规则，以保证主文档成为项目唯一核心产品文档。

### 变更摘要

- 在 `AGENTS.md` 新增“核心产品文档规则”。
- 明确 `REQUIREMENTS.md` 是本项目唯一核心产品文档和需求事实来源。
- 约定后续需求分析、方案设计、代码实现、UI 调整、接口变更、数据库脚本或文档更新前，都必须先参考 `REQUIREMENTS.md`。
- 如新需求、代码实现、设计判断或其它文档与 `REQUIREMENTS.md` 不一致，必须先向用户说明差异并确认，再继续实现。

### 影响范围

- 后续所有项目工作都应以 `REQUIREMENTS.md` 为核心依据。
- 用户确认后的新结论必须同步写回 `REQUIREMENTS.md`，保持文档长期可信。

## 2026-06-29 用户管理模块改为固定功能栏

### 需求来源

用户确认项目内功能设计方向调整：暂时取消功能树配置模块，不再提供单独的功能树配置页面。每个项目内部改为固定功能栏，由用户后续明确提出具体功能页面后，再逐个加入。

本条为最新产品决策；如早期记录中仍出现“功能树配置”“功能树页签”等描述，后续开发以本条为准。

### 变更摘要

- 移除用户管理模块中的“用户 / 项目模块 / 功能树”页签式配置入口。
- 用户管理模块左侧功能栏先新增第一个功能“用户列表”。
- 用户列表展示包含 `admin` 在内的全部用户，并支持分页展示。
- 项目详情页顶部原“用户管理”标题位置改为“返回项目首页”入口，左侧不再保留原返回项目按钮。
- `mihotel`、`ihotel` 等运维项目暂时保留功能栏骨架，具体功能页面等待后续需求补充。

### 影响范围

- 后续不再按“后台配置功能树”的思路扩展项目功能，除非用户重新确认启用配置化。
- 后续新增项目功能时，应直接在对应项目功能栏中增加入口和页面，并同步记录到本文档。

## 2026-06-29 用户列表页增加永久用户维护入口

### 需求来源

用户要求用户列表页面的主内容框向下拉高，去除标题下方说明文案，在标题右侧增加“新增用户”按钮，并生成永久用户的增删改查接口。

### 变更摘要

- 用户列表面板高度调整为接近当前工作区高度，减少页面下方空白。
- 用户列表标题下方说明文字移除，标题区右侧新增“新增用户”按钮。
- 后端新增永久用户专用接口：查询永久用户、创建永久用户、修改永久用户、删除永久用户。
- 永久用户接口仅允许维护 `PERMANENT` 用户，避免误操作 `admin` 或临时用户。
- 前端新增永久用户 CRUD API 封装，后续新增用户弹窗或编辑交互可直接复用。

### 影响范围

- 当前前端仅新增按钮入口，新增用户表单字段和弹窗样式等待后续确认。
- 后续做用户新增、编辑、删除页面交互时，应优先调用永久用户专用接口。

## 2026-06-29 补齐永久用户接口中文注释

### 需求来源

用户反馈新增的后端接口缺少中文注释，要求遵守全局 Java 中文注释规则。

### 变更摘要

- 为永久用户 CRUD Controller 接口补充中文方法注释，说明接口用途、入参和返回值。
- 为永久用户创建、修改、删除、临时用户创建和项目授权更新等 Service 方法补充业务意图注释。
- 为新增的 Repository 数据库读写方法补充中文注释，说明用户主表和项目授权关系的处理边界。

### 影响范围

- 后续新增 Java 接口、业务方法和数据库写入逻辑时，必须同步补充必要中文注释。

## 2026-06-30 用户列表恢复展示 admin

### 需求来源

用户确认用户列表仍需要把 `admin` 查询并展示出来。

### 变更摘要

- 用户列表前端取消对 `admin` 账号的过滤，列表分页统计包含 `admin`。
- 用户列表面板宽度调整为覆盖侧边栏右侧主要工作区，接近用户截图中两条红线标注的横向范围。
- 用户列表中 `admin` 固定排在第一位；当前登录用户所在行只展示“修改密码”按钮，其余用户展示“修改信息”“修改密码”“删除用户”三个按钮。
- 后端新增管理员修改任意用户密码接口，当前未接入会话鉴权，接口通过操作人账号校验管理员身份。
- 前端新增用户、修改用户信息共用一套弹窗模板，字段按后端账号、用户名、启用状态和授权项目编码组织；修改时账号 code 不允许编辑。
- 前端新增修改密码弹窗，仅展示“密码”输入框，并调用管理员修改密码接口。
- 新增用户弹窗的用户类型支持选择永久用户或临时用户；状态支持选择启用或停用，默认启用。
- 新增用户弹窗选择临时用户时，必须联动展示“可用时间（小时）”输入框；该值必须为正整数，并作为当前时间向后叠加的小时数传给后端。
- 新增永久用户和临时用户时，后端默认生成的初始密码统一为 `1234`。
- 授权项目列表去除用户管理入口，用户管理当前仅作为 `admin` 独有功能，后续扩展时再重新设计授权方式。
- 修改密码弹窗中“密码：”标签和输入框保持同一行对齐。
- 用户列表操作列表头和操作按钮需要在操作列中居中对齐。
- 用户列表行内操作按钮使用较小尺寸，避免操作区过重。
- 用户列表中用户名展示列和账号均使用普通字重。
- 用户列表第一列表头以及新增、修改用户弹窗中的“显示名称”文案统一改为“用户名”，仅调整前端展示文案，不改变后端 `displayName` 字段。
- 用户列表接口改为后端分页：`GET /api/ops-console/users?page=1&pageSize=10` 返回当前页记录、总条数、当前页、每页条数和总页数。
- 前端用户列表翻页时重新请求后端分页接口，不再一次性拉取全量用户后在前端 slice 分页。
- 数据库 seed 新增 50 个模拟永久用户 `demo-user-001` 至 `demo-user-050`，默认密码为 `1234`，用于观察用户列表分页显示效果。
- 用户列表删除按钮需要可用：点击后弹出删除确认框，确认后调用通用删除接口，并清理用户项目授权关系。
- 保留永久用户增删改接口仅维护 `PERMANENT` 用户的限制，避免通过永久用户维护入口误操作管理员账号。

### 影响范围

- 用户管理模块的用户列表展示全部账号。
- 用户列表面板使用更宽的项目工作区布局，减少左右空白。
- 后续实现按钮弹窗时，“修改密码”应调用管理员密码修改接口；当前登录用户行不得展示修改信息和删除用户入口。
- 用户新增和修改弹窗已接入保存动作；新增默认选择永久用户，修改统一调用通用用户信息修改接口。
- 后端创建临时用户时不再默认使用 24 小时，必须校验并使用请求中的 `validHours` 正整数值。
- 用户信息修改已新增通用接口，账号和用户类型在修改时不允许变更。
- 用户删除已新增通用接口，禁止删除 `admin` 管理员账号。
- 后续如果新增编辑、删除按钮，需要继续区分 `admin`、永久用户和临时用户的可操作范围。

## 2026-06-30 后端 ORM 统一使用 MyBatis-Plus

### 需求来源

用户发现当前 DAO 层使用 JDBC 操作数据库，要求后续统一使用 MyBatis-Plus，并将该约定记录到 `AGENTS.md`。

### 变更摘要

- `AGENTS.md` 已新增 MyBatis-Plus ORM 规范。
- 后续后端数据库访问统一使用 MyBatis-Plus，不再新增 `JdbcTemplate` 或手写 JDBC DAO。
- 简单 SQL 优先使用 MyBatis-Plus 的 `LambdaQueryWrapper`、`LambdaUpdateWrapper`、`BaseMapper` 等类型安全 API。
- 复杂 SQL 使用 XML Mapper 编写，包括多表关联、复杂动态 SQL、批量统计、复杂排序分页或需要精细调优的 SQL。
- 除非用户有特殊要求，后续所有 SQL 和 ORM 代码都按该规范执行。
- 已将当前 `OpsConsoleRepository` 从 `JdbcTemplate` 迁移为 MyBatis-Plus Mapper 实现。
- 已新增用户、项目、授权、功能节点实体和对应 BaseMapper；用户项目关联查询与 `INSERT IGNORE ... SELECT` 授权写入放入 XML Mapper。

### 影响范围

- 后续新增或改造 DAO / Mapper 时，应优先按 MyBatis-Plus 组织实体、Mapper、Service 和 XML。
- 当前后端源码中不再保留 `JdbcTemplate` DAO；后续维护需要保持接口行为、事务边界和权限过滤逻辑一致。

## 2026-06-30 后端对象统一使用 Lombok

### 需求来源

用户要求本项目启用 Lombok 工具，并将当前项目内已有对象改造为 Lombok 写法，同时把该约定记录到 `AGENTS.md`。

### 变更摘要

- 后端 Maven 依赖新增 `org.projectlombok:lombok`，作为 Java 对象样板代码生成工具。
- `AGENTS.md` 新增 Lombok 使用规范：普通实体类、领域模型、DTO/VO 优先使用 Lombok 减少 getter、setter、构造器等重复代码。
- 当前 MyBatis-Plus 实体对象 `SysUserEntity`、`OpsProjectEntity`、`OpsUserProjectPermissionEntity`、`OpsFunctionNodeEntity` 已改为 Lombok `@Getter`、`@Setter`。
- 当前领域模型 `UserAccount`、`ProjectModule`、`FunctionNode` 已改为 Lombok 生成访问器，保留原有构造器和业务方法。
- 现有 DTO 多数为 Java `record`，已经是简洁不可变数据载体，本次不强行改成 Lombok 类。

### 影响范围

- 后续新增或改造 Java 普通对象时，默认使用 Lombok，避免继续手写重复 getter、setter。
- 对存在继承、集合字段、业务行为或 `equals/hashCode` 风险的对象，不默认使用 `@Data`，优先使用 `@Getter`、`@Setter` 等精确注解。
- 本次改造不改变接口字段、数据库字段映射和业务行为，仅减少 Java 对象样板代码。

## 2026-06-30 分页默认值与接口 Mapping 安全约定调整

### 需求来源

用户要求分页默认值调整为 10，并出于安全考虑，项目后续除 `GetMapping` 和 `PostMapping` 外，其余方法级 Mapping 都改为 `PostMapping`，同时将该约定记录到 `AGENTS.md`。

### 变更摘要

- 用户列表分页默认每页条数从 8 调整为 10，后端 `pageSize` 默认值和前端调用默认值保持一致。
- 当前后端 Controller 中已有 `PutMapping`、`DeleteMapping` 全部调整为 `PostMapping`。
- 删除类接口改为 POST 调用并追加 `/delete` 路径后缀，避免与同资源路径下的修改接口发生 POST 路由冲突。
- 前端 API 封装同步把修改、删除、修改密码等调用改为 `POST`。
- `AGENTS.md` 新增接口 Mapping 安全约定：查询类接口可使用 `@GetMapping`，会改变服务端状态的接口统一使用 `@PostMapping`；不再新增 `@PutMapping`、`@DeleteMapping`、`@PatchMapping` 等其它方法级 Mapping。

### 影响范围

- 用户管理列表默认每页展示 10 条数据。
- 后续新增后端 Controller 方法时，应优先按 GET 查询、POST 变更的方式设计接口。
- 如历史文档或外部调用方仍引用 PUT/DELETE 接口，需要同步改为 POST 新路径。

## 2026-06-30 后端按项目和业务模块拆分类结构

### 需求来源

用户要求将 Controller、Service、Mapper 按不同项目或业务模块拆开，避免后续所有功能都堆积在同一个类中；以后新增项目管理功能时，需要新建对应项目的类，并将该约定记录到 `AGENTS.md`。

### 变更摘要

- 删除泛化的 `OpsConsoleController`、`OpsConsoleService`、`OpsConsoleRepository` 和 `OpsConsoleMapper` 命名边界。
- 用户管理接口拆分为 `UserAdminController`、`UserAdminService`、`UserAdminRepository`。
- 登录校验拆分为独立 `AuthService`，`AuthController` 不再依赖泛化运维控制台服务。
- 项目模块接口拆分为 `ProjectModuleController`、`ProjectModuleService`、`ProjectModuleRepository`。
- 项目功能节点接口拆分为 `FunctionNodeController`、`FunctionNodeService`、`FunctionNodeRepository`。
- 原 XML 中跨用户、项目、授权关系表的复杂 SQL 拆分为 `UserProjectPermissionQueryMapper` 和对应 XML，名称直接表达用户项目授权查询与写入职责。
- 对外 API 路径保持兼容，前端无需因本次类拆分调整调用地址。
- `AGENTS.md` 新增后端模块拆分规范：后续新增 `mihotel`、`ihotel` 或其它项目管理功能时，优先新建对应项目的 Controller、Service、Mapper / Repository 和必要 XML Mapper 文件。
- 根据后续确认，拆分后的 Controller 类级 `@RequestMapping` 不再共用 `/api/ops-console`，改为按模块区分：用户管理 `/api/user-admin`，项目模块 `/api/project-modules`，项目功能节点 `/api/project-function-nodes`。
- 前端 API 封装同步切换到新的模块化接口路径。

### 影响范围

- 后端代码阅读和维护按认证、用户管理、项目模块、项目功能节点分区。
- 后续新增项目管理能力时，不应继续扩展笼统的 `OpsConsole*` 大类，而应按项目或业务模块建立独立类。
- 跨项目复用能力可以保留共享类，但类名必须表达具体职责，避免泛化命名造成职责堆积。
- 后续新增 Controller 时，类级路径也要体现模块或项目名称，避免多个 Controller 共用同一个笼统前缀导致接口归属不清。

## 2026-06-30 清理未使用接口和方法

### 需求来源

用户要求扫描全部项目，删除当前未使用的接口和方法，只保留实际使用的能力，保持项目干净。

### 变更摘要

- 前端删除未被页面调用的 API 封装：永久用户专用查询、永久用户专用修改、永久用户专用删除、功能节点查询和健康检查前端封装。
- 后端删除当前无前端入口且无内部调用链的接口：永久用户专用查询、永久用户专用修改、永久用户专用删除、独立用户项目授权更新、项目创建、项目功能节点查询和项目功能节点创建。
- 删除上述接口对应的未使用 DTO、Service 方法、Repository 方法、Mapper 方法和功能节点相关模型。
- 删除当前代码不再使用的功能节点实体、Mapper、Repository、Service、Controller 和前端类型定义。
- 数据库 schema 与 seed 同步移除 `ops_function_node` 表定义和功能节点初始化数据，避免脚本继续保留当前业务不用的结构。
- 后端 `/api/health` 健康检查接口仍保留，用于启动日志、部署探活和基础运行状态确认；仅删除前端未调用的封装函数。

### 影响范围

- 当前保留的前端 API 与后端接口仅覆盖登录、用户分页、新增用户、修改用户、删除用户、修改密码和项目列表。
- 功能节点配置能力已从当前代码和数据库初始化脚本移除，后续如重新启用，需要按项目模块拆分规范重新新增对应类和脚本。
- 项目创建接口当前没有前端入口，已删除；后续需要项目维护页面时再按模块重新设计并补回。

## 2026-06-30 临时用户增加时间入口

### 需求来源

用户根据用户列表截图指出，临时用户不需要出现“修改信息”功能，应将该位置替换为“增加时间”，点击后弹出输入框，给选中的临时用户快速增加临时时间。

### 变更摘要

- 用户列表中永久用户继续展示“修改信息”按钮。
- 用户列表中临时用户不再展示“修改信息”按钮，改为展示“增加时间”按钮。
- 点击“增加时间”后弹出小型表单，仅填写需要增加的小时数；小时数必须为正整数。
- 新增后端接口 `POST /api/user-admin/users/{username}/temporary-time`，仅允许给临时用户增加有效时间。
- 临时用户续期按 `max(当前时间, 原到期时间) + 增加小时数` 计算；如果账号已过期，则从当前时间重新叠加。
- 后端同步限制通用“修改用户信息”接口只允许修改永久用户，避免绕过前端误改临时用户信息。
- 临时用户新增时不再展示状态字段，前端也不再提交临时用户状态。
- 临时用户登录时只校验有效期，不再校验启用/禁用状态。
- 用户列表状态列中，临时用户直接展示蓝色“临时”标识，区别于普通启用/禁用状态。

### 影响范围

- 临时用户后续维护方式为增加有效时间、修改密码或删除，不再通过用户信息编辑弹窗修改基础信息。
- 永久用户仍可通过原“修改信息”弹窗维护用户名、状态和授权项目。

## 2026-06-30 用户列表到期时间展示优化

### 需求来源

用户根据用户列表截图指出，临时用户到期时间需要展示为 `xxxx-xx-xx xx:xx:xx` 格式，永久用户不展示横杠，改为展示一个好看的“永久”小图标。

### 变更摘要

- 前端用户列表新增时间格式化方法，将后端返回的 ISO 时间格式展示为 `yyyy-MM-dd HH:mm:ss`。
- 永久用户到期时间列改为展示绿色胶囊样式的“永久”标识，带圆形 `∞` 图标。
- 管理员账号展示红色胶囊样式的“永久”标识，永久用户展示绿色胶囊样式的“永久”标识，用于区分管理员永久权限和普通永久用户。
- 到期时间列内容居中对齐，避免标识和时间在视觉上偏离表头。
- 用户类型列改为按类型展示不同颜色的胶囊标签：管理员、永久用户、临时用户分别使用不同视觉样式。
- 状态列改为按账号状态和用户类型展示不同颜色的胶囊标签：管理员状态使用红色，普通启用使用绿色，禁用使用灰色。

### 影响范围

- 仅调整用户列表展示效果，不改变后端字段、数据库结构和接口返回格式。

## 2026-07-02 新增云入住项目模块配置

### 需求来源

用户要求暂时从用户管理模块转移，新增“云入住”模块，并加入当前项目模块配置。

### 变更摘要

- 在项目模块初始化 seed 中新增 `cloud-checkin` 项目配置，名称为“云入住”，图标文本为 `CI`，排序位于 `ihotel` 之后。
- 新增可重复执行的数据库迁移脚本 `database/migration/20260702_001_add_cloud_checkin_project.sql`，用于给已有数据库补充“云入住”项目配置。
- 默认给 `admin` 授权“云入住”模块；模拟用户也默认获得“云入住”访问权限，便于普通用户入口展示验证。
- 当前“云入住”只新增为项目入口模块，内部具体运维功能页面暂不补充，仍沿用项目工作区骨架和“功能模块待补充”状态。

### 影响范围

- 登录后的项目入口页会在用户具备授权时展示“云入住”卡片。
- 用户新增/修改弹窗的授权项目列表会自动出现“云入住”，普通账号可被分配该模块访问权限。
- 后续补充“云入住”具体管理功能时，应按项目模块拆分规范新建对应 Controller、Service、Mapper / Repository 和必要数据库脚本。

## 2026-07-02 云入住增加门店 ROP 信息注册功能入口

### 需求来源

用户要求在“云入住”模块内先增加一个功能，名称为“门店 ROP 信息注册”，后续需要在该功能内继续编写具体业务能力。

### 变更摘要

- 云入住模块左侧固定功能栏新增“门店 ROP 信息注册”入口。
- 点击进入“云入住”模块时，默认选中“门店 ROP 信息注册”。
- 新增独立的“门店 ROP 信息注册”工作区骨架，当前仅保留标题和内容承载区，具体表单、字段、接口与数据库结构等待后续需求补充。

### 影响范围

- 当前变更仅影响前端云入住模块内部功能展示，不新增后端接口和数据库表。
- 后续在该功能内补充真实业务逻辑时，应基于云入住模块单独拆分对应后端 Controller、Service、Mapper / Repository，并同步补充数据库脚本。

## 2026-07-02 登录页初始账号密码留空

### 需求来源

用户根据登录页截图反馈，项目首次打开或访问时不应默认填入账号和密码；但用户登录后再退出登录，可以保留刚才输入过的账号密码。

### 变更摘要

- 登录页 `username` 和 `password` 初始值从 `admin/admin` 调整为空字符串。
- 退出登录逻辑不主动清空 `username` 和 `password`，因此同一次页面会话中登录后再退出时，登录框仍保留用户刚才输入的账号密码。

### 影响范围

- 首次打开登录页不再出现默认账号密码。
- 当前变更仅影响前端登录表单初始状态，不改变认证接口和后端逻辑。

## 2026-07-02 门店 ROP 信息注册页面美化与录入入口

### 需求来源

用户根据示例截图要求完善“门店 ROP 信息注册”页面，整体风格需要与当前项目 UI 保持一致；页面需要支持手动输入集团地址，并支持上传 ROP 认证信息文件或手动输入认证信息文本。

### 变更摘要

- 将“门店 ROP 信息注册”工作区从占位空框调整为正式表单页面。
- 页面新增“集团地址”输入框，用于手动填写集团服务地址。
- 页面新增“ROP 认证信息”区域，支持上传认证文件，也支持打开弹窗手动输入认证信息内容。
- 手动输入弹窗改为项目统一的白底表单风格，文案调整为“手动输入 ROP 认证信息”和“请粘贴 ROP 认证信息内容，支持完整配置文本或认证参数片段”。
- 认证信息区域会展示当前已选择的文件名，或提示已手动输入认证内容。

### 影响范围

- 当前变更仅完成前端页面、弹窗和基础录入状态，不新增后端提交接口、不落库。
- 后续接入真实注册逻辑时，需要为“门店 ROP 信息注册”补充云入住模块专属后端接口、DTO、Service、Mapper / Repository 和数据库脚本。

## 2026-07-02 门店 ROP 信息注册增加集团代码查询联动

### 需求来源

用户要求在“集团地址”上方新增“集团代码”输入框和查询按钮；输入集团代码后调用外部运维系统接口查询集团信息，如果查到数据则取返回结果中的 `url` 填入集团地址；集团地址仍允许人工编辑，便于运维人员修正；重复更换集团代码并查询时，集团地址应被最新查询结果覆盖；未查到时清空集团地址。

### 变更摘要

- 门店 ROP 信息注册页面新增“集团代码”输入框和“查询”按钮。
- 前端新增集团地址查询联动：查询成功时用返回的集团地址覆盖可编辑的“集团地址”输入框；查询不到时清空集团地址并提示手动填写。
- 后端新增云入住专属接口 `GET /api/cloud-checkin/group-address?groupCode=...`，避免前端直接跨域请求外部运维系统。
- 后端按外部短链接口格式请求 `GET http://api.ihotel.cn/s/url/{集团代码}_GROUP`，并解析返回数据 `data[0].url` 字段作为集团地址。
- 已用示例集团代码 `DHYTJSG` 验证新接口可返回 `http://94.191.93.26:8102`。

### 影响范围

- 当前联动只负责辅助查询并填充页面输入框，不会自动保存或落库。
- 外部接口是否可查到数据依赖 `api.ihotel.cn` 网络访问策略；如后续接口地址变化，应优先通过配置项 `cloud-checkin.group-address-query-url-template` 调整。

## 2026-07-02 门店 ROP 信息注册生成注册加密串

### 需求来源

用户要求在门店 ROP 信息注册页面点击“提交注册信息”后，页面底部展示一个注册加密串，并提供更快捷的复制按钮，方便运维人员复制使用。

### 变更摘要

- 点击“提交注册信息”后，前端会基于集团代码、集团地址、ROP 认证信息来源、文件名和认证内容生成注册加密串。
- 注册加密串展示在页面最下方的“注册加密串”结果区。
- 结果区新增“复制加密串”按钮，点击后调用浏览器剪贴板能力复制完整加密串。
- 上传认证文件时会读取文件文本内容；手动输入认证信息时优先使用手动输入内容。

### 影响范围

- 当前加密串为前端基于页面输入生成的 Base64 注册串，用于先满足页面交互和复制流程。
- 后续如果需要接入真实注册加密算法或后端生成接口，应替换当前前端生成逻辑，并保持底部结果展示与复制交互不变。

## 2026-07-02 门店云入住信息结果区交互调整

### 需求来源

用户根据页面截图反馈，点击“提交注册信息”后不要再弹出成功弹窗，直接在页面底部生成结果即可；集团代码通常不超过十位，输入框宽度可以缩短；底部结果区标题需要改为“门店云入住信息”。

### 变更摘要

- 提交注册信息后不再触发“操作成功”弹窗，仅在页面底部生成结果区。
- 集团代码输入框宽度缩短，减少页面横向占用。
- 底部结果区标题从“注册加密串”调整为“门店云入住信息”，复制按钮保留“复制加密串”。

### 影响范围

- 当前变更仅调整前端交互与布局，不改变加密串生成内容和复制逻辑。

## 2026-07-02 门店 ROP 信息注册流程改为先上传认证信息

### 需求来源

用户发现 ROP 认证信息文件中包含 `hotelGroupCode:DHYTJSG` 这类集团代码字段，因此要求调整页面录入顺序：先上传或输入 ROP 认证信息，再根据认证信息中的集团代码自动填充集团信息；如果认证信息中没有 `hotelGroupCode`，再由运维人员手动填写集团代码。

### 变更摘要

- 门店 ROP 信息注册页面步骤顺序调整为：第 1 步“ROP 认证信息”，第 2 步“集团信息”。
- 上传认证文件或确认手动输入认证信息后，前端会解析 `hotelGroupCode` 字段。
- 如果解析到集团代码，则自动填入“集团代码”输入框，并立即调用集团地址查询接口带出集团地址。
- 集团代码和集团地址仍允许运维人员继续手动修改。
- 如果未解析到 `hotelGroupCode`，则清空集团代码和集团地址，并提示运维人员手动填写集团代码；后续查不到地址、查到地址的处理逻辑保持不变。

### 影响范围

- 当前变更仅调整前端录入顺序和自动填充逻辑，不改变后端集团地址查询接口和注册加密串生成结构。

## 2026-07-02 门店云入住信息生成按钮调整

### 需求来源

用户根据页面截图要求，将“提交注册信息”按钮文案改为“生成”，并将按钮尺寸适当缩小。

### 变更摘要

- 门店 ROP 信息注册页面底部操作按钮文案从“提交注册信息”调整为“生成”。
- 缩小该按钮的最小宽度和高度，使其更符合局部生成操作的视觉重量。

### 影响范围

- 当前变更仅调整前端按钮文案和样式，不改变生成逻辑。

## 2026-07-02 门店云入住信息生成规则调整

### 需求来源

用户提供酒店配置示例，要求“门店 ROP 信息注册”页面生成给运维使用的注册信息串，而不是前端临时 Base64 注册加密串。

### 变更摘要

- 门店云入住信息生成格式调整为：`集团地址;集团代码;username;password;appKey;appSecret`。
- `集团地址` 使用页面中可编辑的集团地址输入框当前值；`集团代码` 使用页面中可编辑的集团代码输入框当前值。
- `username`、`password`、`appKey`、`appSecret` 从上传或手动输入的 ROP 认证信息文本中解析。
- 点击“生成”时校验 ROP 认证信息必须包含上述四个字段，缺少字段时在页面提示具体缺少项。
- 底部结果区复制按钮文案从“复制加密串”调整为“复制注册信息”，避免与当前生成内容语义不一致。

### 影响范围

- 当前变更仅影响前端“门店 ROP 信息注册”页面的生成和复制展示逻辑。
- 后端集团地址查询接口、ROP 认证信息上传/手动输入、根据 `hotelGroupCode` 自动带出集团信息的逻辑保持不变。

## 2026-07-02 门店 ROP 信息注册返回重进清空状态

### 需求来源

用户反馈在“云入住”模块内填写或生成过门店云入住信息后，点击“返回项目首页”再重新进入，页面仍保留上一次的上传文件、集团代码、集团地址和生成结果，不符合重新进入功能时的预期。

### 变更摘要

- 新增门店 ROP 信息注册表单状态重置逻辑。
- 点击“返回项目首页”时清空 ROP 认证信息、集团代码、集团地址、查询提示、生成结果和复制提示。
- 重新进入“云入住”模块时再次清空门店 ROP 信息注册页面状态，保证每次进入都是干净表单。

### 影响范围

- 当前变更仅影响前端页面内存状态，不改变登录态、项目授权、后端接口和数据库数据。
- 用户退出登录后保留登录框账号密码的既有逻辑不受影响。

## 2026-07-02 云入住新增地址校验功能入口

### 需求来源

用户要求在“云入住”模块左侧功能树中，于“门店 ROP 信息注册”下面新增一个“地址校验”功能。

### 变更摘要

- 云入住模块左侧功能树从单一“门店 ROP 信息注册”入口扩展为多个功能入口。
- 新增“地址校验”入口，显示在“门店 ROP 信息注册”下方。
- 点击“地址校验”后进入独立工作区，当前先提供与项目风格一致的功能骨架，具体校验字段、校验规则、接口和结果展示待后续需求补充。

### 影响范围

- 当前变更仅影响前端云入住模块功能树和地址校验占位页面。
- 不新增后端接口、数据库脚本或持久化数据。

## 2026-07-02 地址校验支持门店云入住配置查询与生成

### 需求来源

用户要求完善“地址校验”功能：输入门店代码后查询该门店云入住配置，查到后展示并允许手动修改集团地址、user、password、appKey、appSecret，再生成新的配置串并支持复制；未查到时提示该酒店没有云入住配置。

### 变更摘要

- 地址校验页面新增“门店代码”输入框和查询按钮，文案不再使用“集团代码”。
- 后端新增云入住专属接口 `GET /api/cloud-checkin/store-cloud-config?storeCode=...`，由后端代理查询外部短链接口。
- 外部接口请求格式为 `GET http://api.ihotel.cn/s/url/{门店代码}_PMSIPAD`，例如 `LOHKAH001_PMSIPAD`。
- 查询成功时解析外部接口 `data[0].url` 中的分号配置串，格式为：`集团地址;集团代码;user;password;appKey;appSecret`。
- 前端展示并允许修改集团地址、user、password、appKey、appSecret；生成时保留查询结果中的集团代码，重新拼接为新的云入住配置串。
- 外部接口返回 `{"code":200,"msg":"没有 URL 记录！"}`、无 `data` 或无配置内容时，后端统一返回 `found=false`，前端提示“该酒店没有云入住配置”。
- 地址校验结果区提供“复制配置”按钮，方便运维复制生成后的配置串。

### 影响范围

- 当前变更只查询和生成页面配置串，不会写回外部系统或本项目数据库。
- 生成配置依赖原始配置中第二段集团代码；页面暂不暴露集团代码编辑入口，避免运维误改隐藏关键字段。

## 2026-07-02 登录页支持回车提交

### 需求来源

用户反馈登录页在密码输入完成后，希望按 Enter 键可以像点击“登录”按钮一样触发登录操作。

### 变更摘要

- 将登录卡片调整为表单提交结构。
- 登录按钮改为 `submit` 按钮，复用原有登录处理逻辑。
- 用户在用户名或密码输入框中按 Enter 时，会触发表单提交并执行登录。

### 影响范围

- 当前变更仅影响前端登录页交互，不改变认证接口、登录状态保存和退出登录逻辑。
